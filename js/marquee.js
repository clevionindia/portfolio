/**
 * Infinite Logo Marquee Rail
 * Handled via unified track CSS animation (-50% loop) for glitch-free performance.
 */
(function () {
  // Pure CSS animation ensures zero DOM cloning race conditions or overlap bugs.
})();
